conda create -n <venv-name> python=3.10/3.13
conda activate <venv-name>
pip install . -e

## Setup
1. activate venv-mcp-gmail and run "python mcp-gmail/mcp_gmail/server.py"
2. activate venv-email-assistant and run "python -m email_assistant.agent.interface"